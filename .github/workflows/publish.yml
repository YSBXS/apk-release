name: publish

on:
  repository_dispatch:
    types: [apk-update]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: publish-${{ github.event.client_payload.item.tag_name || 'manual' }}
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - env:
          GH_TOKEN: ${{ github.token }}
          ITEM_JSON: ${{ toJSON(github.event.client_payload.item) }}
        run: python scripts/publish.py
